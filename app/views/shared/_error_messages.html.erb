<% if object.errors.any? %>
	<div id="error_explanation">
		<div class = "alert alert-danger">
			<p><%= pluralize(object.errors.count, "error") %> prohibited this form from being saved:</p>
		</div>
		<ul>
			<% object.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>